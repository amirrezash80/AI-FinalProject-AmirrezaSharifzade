# Data Folder

- `data/raw/` برای فایل خام دیتاست (در Git نگه ندارید)
- `data/processed/` برای خروجی‌های پیش‌پردازش (در Git نگه ندارید)

اگر دانلود خودکار کار نکرد، فایل CSV را دستی در `data/raw/` قرار دهید.
